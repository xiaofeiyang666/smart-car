C251 COMPILER V5.60.0,  camera                                                             01/05/26  14:22:08  PAGE 1   


C251 COMPILER V5.60.0, COMPILATION OF MODULE camera
OBJECT MODULE PLACED IN .\out_file\camera.obj
COMPILER INVOKED BY: D:\keil\C251\BIN\C251.EXE ..\code\camera.c LARGE FLOAT64 ROM(HUGE) WARNINGLEVEL(3) OPTIMIZE(0,SIZE)
                    - BROWSE INCDIR(..\..\libraries\zf_common;..\..\libraries\zf_components;..\..\libraries\zf_device;..\..\libraries\zf_driv
                    -er;..\user;..\code;..\..\libraries\zf_components\stc32g_usb_lib;..\..\libraries\zf_components\seekfree_assistant) DEBUG 
                    -PRINT(.\out_file\camera.lst) TABS(2) OBJECT(.\out_file\camera.obj) 

stmt  level    source

    1          #include "camera.h"
    2          #include "zf_common_headfile.h"
    3          
    4          // ===================== 可调参数（视觉层） =====================
    5          #define CAMERA_DEBUG_DRAW_ENABLE         0   // 0=关闭显示提帧率，1=开启显示便于调试
    6          #define CAMERA_DEBUG_DRAW_INTERVAL       6
    7          
    8          // 性能档位：0=稳健，1=均衡，2=高帧率（推荐）
    9          #define CAMERA_PERF_PROFILE              2
   10          
   11          #if (CAMERA_PERF_PROFILE == 0)
               #define CAMERA_LOCAL_RANGE_MIN           14
               #define CAMERA_LOCAL_RANGE_MAX           36
               #define CAMERA_GLOBAL_RESCAN_NEAR_ROW    96
               #define CAMERA_ROW_STEP                  2
               #define CAMERA_MID_FILT_ALPHA_PCT        72
               #define CAMERA_FINAL_MID_STEP_MIN        4
               #define CAMERA_FINAL_MID_STEP_MAX        16
               #define CAMERA_RESCAN_STREAK_TH          1
               #define CAMERA_RESCAN_FORCE_NEAR_ROW     114
               #define CAMERA_RESCAN_CONF_TH            50
               #elif (CAMERA_PERF_PROFILE == 1)
               #define CAMERA_LOCAL_RANGE_MIN           12
               #define CAMERA_LOCAL_RANGE_MAX           32
               #define CAMERA_GLOBAL_RESCAN_NEAR_ROW    106
               #define CAMERA_ROW_STEP                  2
               #define CAMERA_MID_FILT_ALPHA_PCT        70
               #define CAMERA_FINAL_MID_STEP_MIN        5
               #define CAMERA_FINAL_MID_STEP_MAX        17
               #define CAMERA_RESCAN_STREAK_TH          2
               #define CAMERA_RESCAN_FORCE_NEAR_ROW     116
               #define CAMERA_RESCAN_CONF_TH            45
               #else
   34          #define CAMERA_LOCAL_RANGE_MIN           12  // 缩小局部搜索窗口
   35          #define CAMERA_LOCAL_RANGE_MAX           28
   36          #define CAMERA_GLOBAL_RESCAN_NEAR_ROW    112 // 全局补扫只保留最靠近车体的区域
   37          #define CAMERA_ROW_STEP                  3   // 按3行处理，帧率提升最明显
   38          #define CAMERA_MID_FILT_ALPHA_PCT        66
   39          #define CAMERA_FINAL_MID_STEP_MIN        6
   40          #define CAMERA_FINAL_MID_STEP_MAX        18
   41          #define CAMERA_RESCAN_STREAK_TH          2   // 连续丢线才触发全局补扫
   42          #define CAMERA_RESCAN_FORCE_NEAR_ROW     116 // 最近场仍强制补扫，防止贴边漏检
   43          #define CAMERA_RESCAN_CONF_TH            45
   44          #endif
   45          
   46          #define CAMERA_EDGE_GRAD_TH              24  // 灰度梯度阈值：越大越抗噪，越小越灵敏
   47          #define CAMERA_EDGE_STEP_TH              10  // 邻域亮度跳变阈值：越大越保守
   48          #define CAMERA_EDGE_SPAN_TH              20  // 局部对比度阈值：抑制低纹理区域误检
   49          #define CAMERA_EDGE_BIAS_TH              6   // 亮暗偏置阈值：过滤伪边缘
   50          #define CAMERA_TOP_THRESHOLD_GAIN        14  // 仅调试图传二值化使用
   51          
   52          #define CAMERA_WIDTH_MIN                 18
   53          #define CAMERA_WIDTH_MAX                 (MT9V03X_W - 4)
   54          #define CAMERA_EDGE_MIN_GAP              8
   55          
   56          #define CAMERA_GLOBAL_RESCAN_ENABLE      1   // 1=局部丢线后启用全行补扫
C251 COMPILER V5.60.0,  camera                                                             01/05/26  14:22:08  PAGE 2   

   57          
   58          #define CAMERA_WIDTH_FILT_ALPHA_PCT      24
   59          #define CAMERA_MID_SEED_BLEND_NUM        2   // mid_seed = (a*old + new)/(a+1)
   60          
   61          #define CAMERA_NEAR_ROW                  (MT9V03X_H - 5)
   62          
   63          #define WIFI_SSID_TEST                   "Car"
   64          #define WIFI_PASSWORD_TEST               "431431431"
   65          #define WIFI_BOUNDARY_ENABLE             1
   66          #define CAMERA_BINARY_OUTPUT_ENABLE      ((CAMERA_DEBUG_DRAW_ENABLE == 1) || (IPS200_OR_WIFI == 1))
   67          
   68          // ===================== 全局输出 =====================
   69          uint8 img_threshold = 120;
   70          uint8 left_jidian = 1;
   71          uint8 right_jidian = MT9V03X_W - 2;
   72          uint8 left_line_list[MT9V03X_H];
   73          uint8 right_line_list[MT9V03X_H];
   74          uint8 mid_line_list[MT9V03X_H];
   75          uint8 final_mid_line = MID_W;
   76          
   77          int16 camera_bias_raw = 0;
   78          int16 camera_preview_raw = 0;
   79          uint8 camera_valid_line_cnt = 0;
   80          uint8 camera_lost_left_cnt = 0;
   81          uint8 camera_lost_right_cnt = 0;
   82          uint8 camera_confidence = 0;
   83          
   84          // ===================== 模块内部变量 =====================
   85          #if CAMERA_BINARY_OUTPUT_ENABLE
               static uint8 bin_image[MT9V03X_H][MT9V03X_W];
               #endif
   88          static int final_mid_filtered_x8 = MID_W * 8;
   89          static uint8 final_mid_init = 0;
   90          
   91          volatile uint16 current_fps = 0;
   92          volatile uint16 fps_counter = 0;
   93          #if CAMERA_DEBUG_DRAW_ENABLE
               static uint8 skip_draw = 0;
               #endif
   96          
   97          static int abs_i(int x)
   98          {
   99   1          return (x >= 0) ? x : -x;
  100   1      }
  101          
  102          static int clamp_i(int x, int min_v, int max_v)
  103          {
  104   1          if (x < min_v) return min_v;
  105   1          if (x > max_v) return max_v;
  106   1          return x;
  107   1      }
  108          
  109          static uint8 is_left_transition(int row, int x)
  110          {
  111   1          int g0, g1, g2, g3, g4;
  112   1          int local_min;
  113   1          int local_max;
  114   1          int grad;
  115   1          int step;
  116   1          int grad_th;
  117   1      
  118   1          if (x < 2 || x > MT9V03X_W - 3) return 0;
  119   1      
  120   1          g0 = (int)mt9v03x_image[row][x - 2];
  121   1          g1 = (int)mt9v03x_image[row][x - 1];
  122   1          g2 = (int)mt9v03x_image[row][x];
C251 COMPILER V5.60.0,  camera                                                             01/05/26  14:22:08  PAGE 3   

  123   1          g3 = (int)mt9v03x_image[row][x + 1];
  124   1          g4 = (int)mt9v03x_image[row][x + 2];
  125   1      
  126   1          local_min = g0;
  127   1          if (g1 < local_min) local_min = g1;
  128   1          if (g2 < local_min) local_min = g2;
  129   1          if (g3 < local_min) local_min = g3;
  130   1          if (g4 < local_min) local_min = g4;
  131   1      
  132   1          local_max = g0;
  133   1          if (g1 > local_max) local_max = g1;
  134   1          if (g2 > local_max) local_max = g2;
  135   1          if (g3 > local_max) local_max = g3;
  136   1          if (g4 > local_max) local_max = g4;
  137   1      
  138   1          if ((local_max - local_min) < CAMERA_EDGE_SPAN_TH) return 0;
  139   1      
  140   1          grad_th = CAMERA_EDGE_GRAD_TH;
  141   1          if (row >= CAMERA_GLOBAL_RESCAN_NEAR_ROW) grad_th -= 2;
  142   1          if (row <= 32) grad_th += 2;
  143   1          if (grad_th < 12) grad_th = 12;
  144   1      
  145   1          grad = (g2 + g3) - (g0 + g1);
  146   1          step = g2 - g1;
  147   1      
  148   1          if ((grad >= grad_th) &&
  149   1              (step >= CAMERA_EDGE_STEP_TH) &&
  150   1              ((g2 - local_min) >= CAMERA_EDGE_BIAS_TH))
  151   1          {
  152   2              return 1;
  153   2          }
  154   1          return 0;
  155   1      }
  156          
  157          static uint8 is_right_transition(int row, int x)
  158          {
  159   1          int g0, g1, g2, g3, g4;
  160   1          int local_min;
  161   1          int local_max;
  162   1          int grad;
  163   1          int step;
  164   1          int grad_th;
  165   1      
  166   1          if (x < 2 || x > MT9V03X_W - 3) return 0;
  167   1      
  168   1          g0 = (int)mt9v03x_image[row][x - 2];
  169   1          g1 = (int)mt9v03x_image[row][x - 1];
  170   1          g2 = (int)mt9v03x_image[row][x];
  171   1          g3 = (int)mt9v03x_image[row][x + 1];
  172   1          g4 = (int)mt9v03x_image[row][x + 2];
  173   1      
  174   1          local_min = g0;
  175   1          if (g1 < local_min) local_min = g1;
  176   1          if (g2 < local_min) local_min = g2;
  177   1          if (g3 < local_min) local_min = g3;
  178   1          if (g4 < local_min) local_min = g4;
  179   1      
  180   1          local_max = g0;
  181   1          if (g1 > local_max) local_max = g1;
  182   1          if (g2 > local_max) local_max = g2;
  183   1          if (g3 > local_max) local_max = g3;
  184   1          if (g4 > local_max) local_max = g4;
  185   1      
  186   1          if ((local_max - local_min) < CAMERA_EDGE_SPAN_TH) return 0;
  187   1      
  188   1          grad_th = CAMERA_EDGE_GRAD_TH;
C251 COMPILER V5.60.0,  camera                                                             01/05/26  14:22:08  PAGE 4   

  189   1          if (row >= CAMERA_GLOBAL_RESCAN_NEAR_ROW) grad_th -= 2;
  190   1          if (row <= 32) grad_th += 2;
  191   1          if (grad_th < 12) grad_th = 12;
  192   1      
  193   1          grad = (g0 + g1) - (g2 + g3);
  194   1          step = g1 - g2;
  195   1      
  196   1          if ((grad >= grad_th) &&
  197   1              (step >= CAMERA_EDGE_STEP_TH) &&
  198   1              ((local_max - g2) >= CAMERA_EDGE_BIAS_TH))
  199   1          {
  200   2              return 1;
  201   2          }
  202   1          return 0;
  203   1      }
  204          
  205          void mark_frame_processed(void)
  206          {
  207   1          fps_counter++;
  208   1      }
  209          
  210          void my_fps_timer_callback(void)
  211          {
  212   1          static uint16 time_ms = 0;
  213   1          time_ms++;
  214   1          if (time_ms >= 1000)
  215   1          {
  216   2              current_fps = fps_counter;
  217   2              fps_counter = 0;
  218   2              time_ms = 0;
  219   2          }
  220   1      }
  221          
  222          // 灰度快照+二值快照：仅用于调试显示/图传，不参与主寻线
  223          #if CAMERA_BINARY_OUTPUT_ENABLE
               static void make_binary_snapshot(void)
               {
                   uint16 y, x;
                   uint8 th_top;
                   uint8 th_bot;
                   uint16 top_end;
                   uint8 th;
                   uint8 g;
               
                   th_bot = img_threshold;
                   th_top = (img_threshold > (255 - CAMERA_TOP_THRESHOLD_GAIN)) ? 255 : (img_threshold + CAMERA_TOP_THRE
             -SHOLD_GAIN);
                   top_end = MT9V03X_H / 3;
               
                   for (y = 0; y < MT9V03X_H; y++)
                   {
                       th = (y < top_end) ? th_top : th_bot;
                       for (x = 0; x < MT9V03X_W; x++)
                       {
                           g = mt9v03x_image[y][x];
                           bin_image[y][x] = (g >= th) ? 255 : 0;
                       }
                   }
               
               }
               #endif
  249          
  250          // 根据行号估计赛道半宽（近处更宽，远处更窄）
  251          static int estimate_half_width(int row)
  252          {
  253   1          int num;
C251 COMPILER V5.60.0,  camera                                                             01/05/26  14:22:08  PAGE 5   

  254   1          int den;
  255   1          int val;
  256   1      
  257   1          den = (search_start_line - search_end_line);
  258   1          if (den <= 0) den = 1;
  259   1      
  260   1          num = (row - search_end_line);
  261   1          if (num < 0) num = 0;
  262   1          if (num > den) num = den;
  263   1      
  264   1          // 远处约44，近处约78
  265   1          val = 44 + (34 * num) / den;
  266   1          val = clamp_i(val, 42, 80);
  267   1          return val;
  268   1      }
  269          
  270          // 局部窗口找左边缘：优先贴近ref_x，减小跳变
  271          static int find_left_edge_local(int row, int ref_x, int range)
  272          {
  273   1          int x;
  274   1          int start_x;
  275   1          int end_x;
  276   1          int best_x;
  277   1          int best_dist;
  278   1          int dist;
  279   1      
  280   1          start_x = clamp_i(ref_x - range, 2, MT9V03X_W - 3);
  281   1          end_x = clamp_i(ref_x + range, 2, MT9V03X_W - 3);
  282   1      
  283   1          best_x = -1;
  284   1          best_dist = 10000;
  285   1      
  286   1          for (x = start_x; x <= end_x; x++)
  287   1          {
  288   2              if (is_left_transition(row, x))
  289   2              {
  290   3                  dist = abs_i(x - ref_x);
  291   3                  if (dist < best_dist)
  292   3                  {
  293   4                      best_dist = dist;
  294   4                      best_x = x;
  295   4                  }
  296   3              }
  297   2          }
  298   1      
  299   1          return best_x;
  300   1      }
  301          
  302          // 局部窗口找右边缘：优先贴近ref_x，减小跳变
  303          static int find_right_edge_local(int row, int ref_x, int range)
  304          {
  305   1          int x;
  306   1          int start_x;
  307   1          int end_x;
  308   1          int best_x;
  309   1          int best_dist;
  310   1          int dist;
  311   1      
  312   1          start_x = clamp_i(ref_x - range, 2, MT9V03X_W - 3);
  313   1          end_x = clamp_i(ref_x + range, 2, MT9V03X_W - 3);
  314   1      
  315   1          best_x = -1;
  316   1          best_dist = 10000;
  317   1      
  318   1          for (x = start_x; x <= end_x; x++)
  319   1          {
C251 COMPILER V5.60.0,  camera                                                             01/05/26  14:22:08  PAGE 6   

  320   2              if (is_right_transition(row, x))
  321   2              {
  322   3                  dist = abs_i(x - ref_x);
  323   3                  if (dist < best_dist)
  324   3                  {
  325   4                      best_dist = dist;
  326   4                      best_x = x;
  327   4                  }
  328   3              }
  329   2          }
  330   1      
  331   1          return best_x;
  332   1      }
  333          
  334          // 全行补扫左边缘：用于大角度弯道局部搜索失败兜底
  335          static int find_left_edge_global(int row)
  336          {
  337   1          int x;
  338   1          for (x = 2; x <= MT9V03X_W - 3; x++)
  339   1          {
  340   2              if (is_left_transition(row, x))
  341   2              {
  342   3                  return x;
  343   3              }
  344   2          }
  345   1          return -1;
  346   1      }
  347          
  348          // 全行补扫右边缘：用于大角度弯道局部搜索失败兜底
  349          static int find_right_edge_global(int row)
  350          {
  351   1          int x;
  352   1          for (x = MT9V03X_W - 3; x >= 2; x--)
  353   1          {
  354   2              if (is_right_transition(row, x))
  355   2              {
  356   3                  return x;
  357   3              }
  358   2          }
  359   1          return -1;
  360   1      }
  361          
  362          // 整行搜索基点，保证大弯贴边时也能起步
  363          void find_jidian(void)
  364          {
  365   1          int y;
  366   1          int left_x;
  367   1          int right_x;
  368   1      
  369   1          y = jidian_search_line - 1;
  370   1          y = clamp_i(y, search_end_line + 1, MT9V03X_H - 1);
  371   1      
  372   1          left_x = find_left_edge_global(y);
  373   1          right_x = find_right_edge_global(y);
  374   1      
  375   1          if (left_x < 0) left_x = 1;
  376   1          if (right_x < 0) right_x = MT9V03X_W - 2;
  377   1      
  378   1          left_jidian = (uint8)left_x;
  379   1          right_jidian = (uint8)right_x;
  380   1      }
  381          
  382          // 主寻线：逐行提取左右边界，并在单边丢线时做宽度模型重建
  383          void image_deal(void)
  384          {
  385   1          int row;
C251 COMPILER V5.60.0,  camera                                                             01/05/26  14:22:08  PAGE 7   

  386   1          int left_now;
  387   1          int right_now;
  388   1          int left_prev;
  389   1          int right_prev;
  390   1          int mid_seed;
  391   1          int left_found;
  392   1          int right_found;
  393   1          int width_meas;
  394   1          int width_est;
  395   1          int width_filtered;
  396   1          int both_valid;
  397   1          int total_rows;
  398   1          int mid_now;
  399   1          int default_half;
  400   1          int local_range;
  401   1          int left_rebuilt;
  402   1          int right_rebuilt;
  403   1          int fix_mid;
  404   1          int fix_half;
  405   1          int fill_row;
  406   1          int miss_streak;
  407   1          uint8 prev_low_conf;
  408   1      
  409   1          for (row = 0; row < MT9V03X_H; row++)
  410   1          {
  411   2              left_line_list[row] = 1;
  412   2              right_line_list[row] = MT9V03X_W - 2;
  413   2              mid_line_list[row] = MID_W;
  414   2          }
  415   1      
  416   1          camera_valid_line_cnt = 0;
  417   1          camera_lost_left_cnt = 0;
  418   1          camera_lost_right_cnt = 0;
  419   1          total_rows = 0;
  420   1      
  421   1          left_prev = (int)left_jidian;
  422   1          right_prev = (int)right_jidian;
  423   1      
  424   1          if ((right_prev - left_prev) < CAMERA_EDGE_MIN_GAP)
  425   1          {
  426   2              left_prev = MID_W - 30;
  427   2              right_prev = MID_W + 30;
  428   2          }
  429   1      
  430   1          left_prev = clamp_i(left_prev, 1, MT9V03X_W - 8);
  431   1          right_prev = clamp_i(right_prev, left_prev + CAMERA_EDGE_MIN_GAP, MT9V03X_W - 2);
  432   1          width_filtered = right_prev - left_prev;
  433   1          mid_seed = (left_prev + right_prev) / 2;
  434   1          miss_streak = 0;
  435   1          prev_low_conf = (camera_confidence <= CAMERA_RESCAN_CONF_TH) ? 1 : 0;
  436   1      
  437   1          for (row = search_start_line - 1; row > search_end_line; row -= CAMERA_ROW_STEP)
  438   1          {
  439   2              total_rows += CAMERA_ROW_STEP;
  440   2              both_valid = 0;
  441   2              left_rebuilt = 0;
  442   2              right_rebuilt = 0;
  443   2      
  444   2              local_range = 18 + (search_start_line - row) / 5;
  445   2              local_range = clamp_i(local_range, CAMERA_LOCAL_RANGE_MIN, CAMERA_LOCAL_RANGE_MAX);
  446   2      
  447   2              left_found = find_left_edge_local(row, left_prev, local_range);
  448   2              right_found = find_right_edge_local(row, right_prev, local_range);
  449   2      
  450   2              // 局部都没找到时，用中线种子做一次扩大窗口搜索
  451   2              if ((left_found < 0) && (right_found < 0))
C251 COMPILER V5.60.0,  camera                                                             01/05/26  14:22:08  PAGE 8   

  452   2              {
  453   3                  left_found = find_left_edge_local(row, mid_seed, local_range + 10);
  454   3                  right_found = find_right_edge_local(row, mid_seed, local_range + 10);
  455   3              }
  456   2      
  457   2              if ((left_found < 0) || (right_found < 0))
  458   2              {
  459   3                  miss_streak++;
  460   3              }
  461   2              else
  462   2              {
  463   3                  miss_streak = 0;
  464   3              }
  465   2      
  466   2      #if CAMERA_GLOBAL_RESCAN_ENABLE
  467   2              if ((row >= CAMERA_GLOBAL_RESCAN_NEAR_ROW) &&
  468   2                  ((miss_streak >= CAMERA_RESCAN_STREAK_TH) ||
  469   2                   (row >= CAMERA_RESCAN_FORCE_NEAR_ROW) ||
  470   2                   prev_low_conf))
  471   2              {
  472   3                  if (left_found < 0)
  473   3                  {
  474   4                      left_found = find_left_edge_global(row);
  475   4                  }
  476   3                  if (right_found < 0)
  477   3                  {
  478   4                      right_found = find_right_edge_global(row);
  479   4                  }
  480   3              }
  481   2      #endif
  482   2      
  483   2              if ((left_found >= 0) && (right_found >= 0) && ((right_found - left_found) >= CAMERA_EDGE_MIN_GAP
             -))
  484   2              {
  485   3                  left_now = left_found;
  486   3                  right_now = right_found;
  487   3                  both_valid = 1;
  488   3              }
  489   2              else if ((left_found >= 0) && (right_found < 0))
  490   2              {
  491   3                  width_est = width_filtered;
  492   3                  left_now = left_found;
  493   3                  right_now = left_found + width_est;
  494   3                  right_rebuilt = 1;
  495   3              }
  496   2              else if ((right_found >= 0) && (left_found < 0))
  497   2              {
  498   3                  width_est = width_filtered;
  499   3                  right_now = right_found;
  500   3                  left_now = right_found - width_est;
  501   3                  left_rebuilt = 1;
  502   3              }
  503   2              else
  504   2              {
  505   3                  default_half = estimate_half_width(row);
  506   3                  mid_now = mid_seed;
  507   3                  left_now = mid_now - default_half;
  508   3                  right_now = mid_now + default_half;
  509   3                  left_rebuilt = 1;
  510   3                  right_rebuilt = 1;
  511   3              }
  512   2      
  513   2              left_now = clamp_i(left_now, 1, MT9V03X_W - 3);
  514   2              right_now = clamp_i(right_now, 2, MT9V03X_W - 2);
  515   2      
  516   2              if ((right_now - left_now) < CAMERA_EDGE_MIN_GAP)
C251 COMPILER V5.60.0,  camera                                                             01/05/26  14:22:08  PAGE 9   

  517   2              {
  518   3                  fix_mid = (left_now + right_now) / 2;
  519   3                  fix_half = estimate_half_width(row);
  520   3                  left_now = clamp_i(fix_mid - fix_half, 1, MT9V03X_W - 3);
  521   3                  right_now = clamp_i(fix_mid + fix_half, 2, MT9V03X_W - 2);
  522   3                  if (left_found < 0) left_rebuilt = 1;
  523   3                  if (right_found < 0) right_rebuilt = 1;
  524   3              }
  525   2      
  526   2              width_meas = right_now - left_now;
  527   2              if ((width_meas >= CAMERA_WIDTH_MIN) && (width_meas <= CAMERA_WIDTH_MAX))
  528   2              {
  529   3                  width_filtered = (int)(((long)width_filtered * (100 - CAMERA_WIDTH_FILT_ALPHA_PCT) + (long)wi
             -dth_meas * CAMERA_WIDTH_FILT_ALPHA_PCT + 50) / 100);
  530   3              }
  531   2      
  532   2              left_line_list[row] = (uint8)left_now;
  533   2              right_line_list[row] = (uint8)right_now;
  534   2              mid_now = (left_now + right_now) / 2;
  535   2              mid_line_list[row] = (uint8)clamp_i(mid_now, 1, MT9V03X_W - 2);
  536   2      
  537   2              // 行步进时，把中间未处理行用当前结果填充，保持中线连续
  538   2              if (CAMERA_ROW_STEP > 1)
  539   2              {
  540   3                  for (fill_row = row + 1; (fill_row < row + CAMERA_ROW_STEP) && (fill_row < search_start_line)
             -; fill_row++)
  541   3                  {
  542   4                      if (fill_row > search_end_line)
  543   4                      {
  544   5                          left_line_list[fill_row] = left_line_list[row];
  545   5                          right_line_list[fill_row] = right_line_list[row];
  546   5                          mid_line_list[fill_row] = mid_line_list[row];
  547   5                      }
  548   4                  }
  549   3              }
  550   2      
  551   2              if (both_valid)
  552   2              {
  553   3                  if (camera_valid_line_cnt <= (uint8)(255 - CAMERA_ROW_STEP))
  554   3                  {
  555   4                      camera_valid_line_cnt += CAMERA_ROW_STEP;
  556   4                  }
  557   3                  else
  558   3                  {
  559   4                      camera_valid_line_cnt = 255;
  560   4                  }
  561   3              }
  562   2              if (left_rebuilt)
  563   2              {
  564   3                  if (camera_lost_left_cnt < 255) camera_lost_left_cnt++;
  565   3              }
  566   2              if (right_rebuilt)
  567   2              {
  568   3                  if (camera_lost_right_cnt < 255) camera_lost_right_cnt++;
  569   3              }
  570   2      
  571   2              left_prev = left_now;
  572   2              right_prev = right_now;
  573   2      
  574   2              mid_seed = (CAMERA_MID_SEED_BLEND_NUM * mid_seed + mid_now) / (CAMERA_MID_SEED_BLEND_NUM + 1);
  575   2              mid_seed = clamp_i(mid_seed, 2, MT9V03X_W - 3);
  576   2          }
  577   1      
  578   1          if (total_rows <= 0)
  579   1          {
  580   2              camera_confidence = 0;
C251 COMPILER V5.60.0,  camera                                                             01/05/26  14:22:08  PAGE 10  

  581   2          }
  582   1          else
  583   1          {
  584   2              int conf;
  585   2              conf = (int)camera_valid_line_cnt * 100 / total_rows;
  586   2      
  587   2              // 丢线越多，置信度越低
  588   2              conf -= (int)camera_lost_left_cnt / 2;
  589   2              conf -= (int)camera_lost_right_cnt / 2;
  590   2              conf = clamp_i(conf, 0, 100);
  591   2              camera_confidence = (uint8)conf;
  592   2          }
  593   1      }
  594          
  595          // 根据中线数组计算最终中线输出，并生成给control的视觉特征
  596          uint8 find_mid_line_weight(void)
  597          {
  598   1          int row;
  599   1          uint32 sum_mid = 0;
  600   1          uint32 sum_w = 0;
  601   1          int width_now;
  602   1          int row_weight;
  603   1          int near_y;
  604   1          int far_y;
  605   1          int mid_new;
  606   1          int rough_preview;
  607   1          int abs_preview;
  608   1          int step_limit;
  609   1          int32 delta_x8;
  610   1          int32 target_mid_x8;
  611   1          int preview_tmp;
  612   1          uint8 final_mid_tmp;
  613   1          int16 bias_tmp;
  614   1          int16 preview_out;
  615   1          uint8 ea_state;
  616   1      
  617   1          near_y = clamp_i(CAMERA_NEAR_ROW, search_end_line + 1, MT9V03X_H - 1);
  618   1      
  619   1          // 先用固定行做一版粗前瞻，再动态决定far_y
  620   1          rough_preview = (int)mid_line_list[32] - (int)mid_line_list[near_y];
  621   1          abs_preview = abs_i(rough_preview);
  622   1      
  623   1          if (abs_preview >= 12)
  624   1          {
  625   2              far_y = 38; // 急弯时略靠近，减少远处噪声误导
  626   2          }
  627   1          else if (abs_preview >= 6)
  628   1          {
  629   2              far_y = 34;
  630   2          }
  631   1          else
  632   1          {
  633   2              far_y = 28; // 直道/缓弯时看得更远，提前预判
  634   2          }
  635   1      
  636   1          if (camera_confidence < 50)
  637   1          {
  638   2              far_y += 4; // 低置信度时回收前瞻距离，优先稳定
  639   2          }
  640   1      
  641   1          far_y = clamp_i(far_y, search_end_line + 1, MT9V03X_H - 1);
  642   1      
  643   1          for (row = search_start_line - 1; row > search_end_line; row -= CAMERA_ROW_STEP)
  644   1          {
  645   2              if (row >= (MT9V03X_H - 10)) row_weight = 18;
  646   2              else if (row >= (MT9V03X_H - 24)) row_weight = 14;
C251 COMPILER V5.60.0,  camera                                                             01/05/26  14:22:08  PAGE 11  

  647   2              else if (row >= 80) row_weight = 10;
  648   2              else if (row >= 60) row_weight = 6;
  649   2              else row_weight = 3;
  650   2      
  651   2              width_now = (int)right_line_list[row] - (int)left_line_list[row];
  652   2              if ((width_now < CAMERA_WIDTH_MIN) || (width_now > CAMERA_WIDTH_MAX))
  653   2              {
  654   3                  row_weight = (row_weight * 5) / 10;
  655   3              }
  656   2      
  657   2              if ((left_line_list[row] <= 2) || (right_line_list[row] >= MT9V03X_W - 3))
  658   2              {
  659   3                  row_weight = (row_weight * 7) / 10;
  660   3              }
  661   2      
  662   2              sum_mid += (uint32)mid_line_list[row] * (uint32)row_weight;
  663   2              sum_w += (uint32)row_weight;
  664   2          }
  665   1      
  666   1          if (sum_w == 0)
  667   1          {
  668   2              mid_new = MID_W;
  669   2          }
  670   1          else
  671   1          {
  672   2              mid_new = (int)(sum_mid / sum_w);
  673   2          }
  674   1      
  675   1          mid_new = clamp_i(mid_new, 1, MT9V03X_W - 2);
  676   1      
  677   1          if (!final_mid_init)
  678   1          {
  679   2              final_mid_filtered_x8 = mid_new * 8;
  680   2              final_mid_init = 1;
  681   2          }
  682   1          else
  683   1          {
  684   2              step_limit = CAMERA_FINAL_MID_STEP_MIN + abs_preview / 2;
  685   2              step_limit = clamp_i(step_limit, CAMERA_FINAL_MID_STEP_MIN, CAMERA_FINAL_MID_STEP_MAX);
  686   2      
  687   2              delta_x8 = (int32)mid_new * 8 - (int32)final_mid_filtered_x8;
  688   2              if (delta_x8 > (int32)step_limit * 8) delta_x8 = (int32)step_limit * 8;
  689   2              if (delta_x8 < -((int32)step_limit * 8)) delta_x8 = -((int32)step_limit * 8);
  690   2      
  691   2              target_mid_x8 = (int32)final_mid_filtered_x8 + delta_x8;
  692   2              final_mid_filtered_x8 = (int)(((int32)final_mid_filtered_x8 * (100 - CAMERA_MID_FILT_ALPHA_PCT) +
             - target_mid_x8 * CAMERA_MID_FILT_ALPHA_PCT + 50) / 100);
  693   2          }
  694   1      
  695   1          final_mid_tmp = (uint8)clamp_i((final_mid_filtered_x8 + 4) / 8, 1, MT9V03X_W - 2);
  696   1          bias_tmp = (int16)((int)final_mid_tmp - MID_W);
  697   1          preview_tmp = (int)mid_line_list[far_y] - (int)mid_line_list[near_y];
  698   1          preview_tmp = clamp_i(preview_tmp, -90, 90);
  699   1          preview_out = (int16)preview_tmp;
  700   1      
  701   1          ea_state = EA;
  702   1          EA = 0;
  703   1          final_mid_line = final_mid_tmp;
  704   1          camera_bias_raw = bias_tmp;
  705   1          camera_preview_raw = preview_out;
  706   1          EA = ea_state;
  707   1      
  708   1          return final_mid_tmp;
  709   1      }
  710          
  711          #if CAMERA_DEBUG_DRAW_ENABLE
C251 COMPILER V5.60.0,  camera                                                             01/05/26  14:22:08  PAGE 12  

               static void draw_debug_overlay(void)
               {
                   int row;
                   int px, py;
               
                   ips200_show_gray_image(0, 0, bin_image[0], MT9V03X_W, MT9V03X_H, 211, 135, 0);
               
                   for (row = search_start_line - 1; row > search_end_line; row -= CAMERA_ROW_STEP)
                   {
                       px = (left_line_list[row] * 9) >> 3;
                       py = (row * 9) >> 3;
                       ips200_draw_point(px, py, RGB565_BLUE);
               
                       px = (right_line_list[row] * 9) >> 3;
                       ips200_draw_point(px, py, RGB565_GREEN);
               
                       px = (mid_line_list[row] * 9) >> 3;
                       ips200_draw_point(px, py, RGB565_RED);
                   }
               
                   ips200_show_string(10, 160, "mid:");
                   ips200_show_uint8(60, 160, final_mid_line);
                   ips200_show_string(10, 175, "fps:");
                   ips200_show_uint16(60, 175, current_fps);
                   ips200_show_string(10, 190, "conf:");
                   ips200_show_uint8(60, 190, camera_confidence);
               }
               #endif
  740          
  741          #if (IPS200_OR_WIFI == 0)
  742          
  743          void camara_init(void)
  744          {
  745   1          ips200_init();
  746   1          ips200_show_string(0, 0, "mt9v03x init.");
  747   1      
  748   1          while (1)
  749   1          {
  750   2              system_delay_ms(80);
  751   2              if (mt9v03x_init())
  752   2              {
  753   3                  ips200_show_string(0, 16, "mt9v03x reinit.");
  754   3              }
  755   2              else
  756   2              {
  757   3                  break;
  758   3              }
  759   2          }
  760   1      
  761   1          ips200_show_string(0, 16, "init success.");
  762   1      }
  763          
  764          void camara_task(void)
  765          {
  766   1          if (mt9v03x_finish_flag)
  767   1          {
  768   2              mt9v03x_finish_flag = 0;
  769   2      
  770   2              find_jidian();
  771   2              image_deal();
  772   2              (void)find_mid_line_weight();
  773   2              mark_frame_processed();
  774   2      
  775   2      #if CAMERA_DEBUG_DRAW_ENABLE
                       if (++skip_draw >= CAMERA_DEBUG_DRAW_INTERVAL)
                       {
C251 COMPILER V5.60.0,  camera                                                             01/05/26  14:22:08  PAGE 13  

                           skip_draw = 0;
               #if CAMERA_BINARY_OUTPUT_ENABLE
                           make_binary_snapshot();
               #endif
                           draw_debug_overlay();
                       }
               #endif
  785   2          }
  786   1      }
  787          
  788          
  789          #elif (IPS200_OR_WIFI == 1)
               
               void camara_init(void)
               {
                   wireless_uart_init();
               
                   while (wifi_spi_init(WIFI_SSID_TEST, WIFI_PASSWORD_TEST))
                   {
                       system_delay_ms(100);
                   }
               
                   if (1 != WIFI_SPI_AUTO_CONNECT)
                   {
                       while (wifi_spi_socket_connect("TCP", WIFI_SPI_TARGET_IP, WIFI_SPI_TARGET_PORT, WIFI_SPI_LOCAL_PO
             -RT))
                       {
                           system_delay_ms(100);
                       }
                   }
               
                   while (mt9v03x_init())
                   {
                       system_delay_ms(80);
                   }
               
                   seekfree_assistant_interface_init(SEEKFREE_ASSISTANT_WIFI_SPI);
                   seekfree_assistant_camera_information_config(SEEKFREE_ASSISTANT_MT9V03X, bin_image[0], MT9V03X_W, MT9
             -V03X_H);
               
               #if WIFI_BOUNDARY_ENABLE
                   seekfree_assistant_camera_boundary_config(X_BOUNDARY, MT9V03X_H, left_line_list, right_line_list, mid
             -_line_list, NULL, NULL, NULL);
               #endif
               }
               
               void camara_task(void)
               {
                   if (mt9v03x_finish_flag)
                   {
                       mt9v03x_finish_flag = 0;
               
                       find_jidian();
                       image_deal();
                       (void)find_mid_line_weight();
                       mark_frame_processed();
               
               #if CAMERA_BINARY_OUTPUT_ENABLE
                       make_binary_snapshot();
               #endif
                       seekfree_assistant_camera_send();
                   }
               }
               
               
               #else
C251 COMPILER V5.60.0,  camera                                                             01/05/26  14:22:08  PAGE 14  

               #error "IPS200_OR_WIFI must be 0 or 1."
               #endif
  843          
  844          
  845          
  846          
  847          
  848          
  849          
  850          
  851          
  852          
  853          
  854          
  855          
  856          
  857          
  858          
  859          
  860          
  861          
  862          
  863          
  864          
  865          


Module Information          Static   Overlayable
------------------------------------------------
  code size            =    ------     ------
  ecode size           =      6015     ------
  data size            =    ------     ------
  idata size           =    ------     ------
  pdata size           =    ------     ------
  xdata size           =       584     ------
  xdata-const size     =    ------     ------
  edata size           =    ------     ------
  bit size             =    ------     ------
  ebit size            =    ------     ------
  bitaddressable size  =    ------     ------
  ebitaddressable size =    ------     ------
  far data size        =    ------     ------
  huge data size       =    ------     ------
  const size           =    ------     ------
  hconst size          =       140     ------
End of Module Information.


C251 COMPILATION COMPLETE.  0 WARNING(S),  0 ERROR(S)
